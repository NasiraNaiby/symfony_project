{% extends 'base.html.twig' %}

{% block title %}Pigeons{% endblock %}

{% block body %}
    <h1>Pigeons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pigeon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pigeon.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ pigeon.color }}</td>
            </tr>
            <tr>
                <th>Pigeon Image </th>
                <!-- <td>{{ pigeon.imgSrc }}</td> -->
                <td><img src="{{ asset('uploads/' ~  pigeon.imgSrc ) }}" style="width: 300px;"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pigeons_index') }}">back to list</a>

    <a href="{{ path('app_pigeons_edit', {'id': pigeon.id}) }}">edit</a>

    {{ include('pigeons/_delete_form.html.twig') }}
{% endblock %}
